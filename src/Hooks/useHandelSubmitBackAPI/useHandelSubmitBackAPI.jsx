import React from "react";
import useCashReciveAdd from "../useCashReciveAdd";

const useHandelSubmitBackAPI = () => {
  const { borrowCashReturnData } = useCashReciveAdd();
  const

  return {};
};

export default useHandelSubmitBackAPI;
